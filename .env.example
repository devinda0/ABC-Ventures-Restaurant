# -----------------------------------------------
# ABC Ventures Restaurant – Environment Template
# -----------------------------------------------
# Copy this file to `.env.local` for local Next.js development
# or to `.env` when running inside Docker/production, then update
# the placeholder values with your own secrets.

# =========================================================================
# Database Configuration
# =========================================================================
# Prisma uses this connection string to reach your MySQL instance.
# Supported formats: https://pris.ly/d/connection-strings
DATABASE_URL="mysql://restaurant_user:restaurant_password@localhost:3306/restaurant_db"

# =========================================================================
# Application & Auth URLs
# =========================================================================
# Public API base: leave empty to fall back to the built-in /api routes.
NEXT_PUBLIC_API_BASE_URL="http://localhost:3000/api"
API_BASE_URL="http://localhost:3000/api"

# Origin URLs used when the app needs to generate absolute links on the server.
NEXT_PUBLIC_SITE_URL="http://localhost:3000"
SITE_URL="http://localhost:3000"
APP_URL="http://localhost:3000"

# =========================================================================
# Stripe Configuration
# =========================================================================
# Replace these with your actual Stripe API keys or use Stripe CLI/stripe-mock.
STRIPE_SECRET_KEY="sk_test_your_secret_key"
STRIPE_PUBLISHABLE_KEY="pk_test_your_publishable_key"
STRIPE_WEBHOOK_SECRET="whsec_your_webhook_secret"
# Override only when pointing to a mock server such as stripe-mock.
STRIPE_API_BASE="https://api.stripe.com"

# =========================================================================
# Email / SMTP Settings
# =========================================================================
# These values power the contact form mailer (via Nodemailer).
SMTP_HOST="smtp.yourprovider.com"
SMTP_PORT="587"
SMTP_USER="your_smtp_username"
SMTP_PASSWORD="your_smtp_password"
SENDER_EMAIL="noreply@yourdomain.com"

# =========================================================================
# Optional Docker Overrides
# =========================================================================
# Uncomment and align with `docker-compose.yml` if you want Docker to read from
# an `.env` file instead of hard-coded values in the compose file.
# MYSQL_ROOT_PASSWORD="rootpassword"
# MYSQL_DATABASE="restaurant_db"
# MYSQL_USER="restaurant_user"
# MYSQL_PASSWORD="restaurant_password"
# NODE_ENV="development"
# PORT="3000"
# HOSTNAME="0.0.0.0"

# =========================================================================
# Deployment Tips
# =========================================================================
# • Vercel automatically injects VERCEL_URL; you can omit the *_SITE_URL vars.
# • Generate `NEXTAUTH_SECRET` with `openssl rand -hex 32` or `npx next-auth@latest secret`.
# • Never commit a real `.env` file—use this template to share required keys.
